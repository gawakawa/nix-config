# AGENTS.md

Guidance for coding agents (Claude Code, Codex) in this repository.

## Critical constraints

- **System rebuilds need sudo — Claude must NOT run them.** The user runs these manually after config changes:
  - NixOS: `sudo nixos-rebuild switch --flake ".#nixos" --accept-flake-config --impure`
  - Darwin: `sudo darwin-rebuild switch --flake ".#mac"`
- **`hardware-configuration.nix`** is auto-generated — never edit it (statix/deadnix ignore it too).
- **Neovim** is a separate flake at `./nvim/`; run via `nix run "$NIXCFG_DIR/nvim" --` (alias `v`).
- **`~/.claude/` and `~/.codex/` are generated by Nix, not hand-edited.** Shared instructions
  (`AGENTS.md`) and skills live under `profiles/home/agents/`; Claude Code-only settings
  (`settings.json`, agents, scripts) live under `profiles/home/claude/`; Codex-only settings live
  under `profiles/home/codex/`. Edit those sources — NOT `~/.claude/` or `~/.codex/` directly
  (both are overwritten on rebuild).
- New files are invisible to flake eval until `git add`-ed.

## Commands Claude can run

- Format: `nix fmt` · Check: `nix flake check` (includes treefmt format check)
- Pre-commit hooks auto-load via direnv; defined in `flakes/pre-commit.nix`.

## Local repos (ghq + gwq)

- ghq clones flat to `~/projects/github.com/<owner>/<repo>`.
- gwq manages worktrees beside them at `~/projects/github.com/<owner>/<repo>=<branch>`.

## Layout

flake-parts config for NixOS (x86_64-linux) + Darwin (aarch64-darwin).

- `flakes/` — flake-parts modules. Host defs in `flakes/hosts.nix`; `mkNixos`/`mkDarwin` helpers in `flakes/lib/hosts.nix`.
- `hosts/`, `home/` — per-host system / Home Manager entry points (`mac`, `nixos`).
- `profiles/` — shared modules (`home/`, `hosts/`). Shell aliases/functions live in `profiles/home/zsh/`.
- CI (`.github/workflows/ci.yml`): `nix flake check` + build both configs, cached via Cachix (`gawakawa`).

## Neovim (./nvim/)

Nix flake-based Neovim config; lazy.nvim handles runtime loading, Nix pins plugin sources declaratively.

- Plugin flow: `nvim/nix/pkgs/vim-plugins/default.nix` (declare sources) → `nvim/nix/plugins.nix`
  (normalize names, e.g. `nvim-treesitter` → `nvim_treesitter`) → `nvim/nix/lib/make-neovim-wrapper.nix`
  (substitute `@plugin_name@` placeholders with store paths) → `nvim/nvim/lua/plugins/*.lua`
  (lazy.nvim spec, `dir = "@plugin_name@"`).
- External tools (LSPs, formatters, linters) declared in `nvim/nix/tools.nix`, added to PATH by the wrapper.
- Lua layout: `nvim/nvim/init.lua` (entry point), `nvim/nvim/lua/plugins/` (one file per plugin),
  `nvim/nvim/lua/config/` (shared config).
- Lint: selene (`nvim/selene.toml`, `nvim/vim.yml`); `mixed_table` is allowed for lazy.nvim specs.
